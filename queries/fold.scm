; Block structures
(block) @fold

; Function definitions
(function_definition) @fold

; Control flow blocks
(if_statement) @fold
(while_loop) @fold
(for_loop) @fold

; Object literals
(object_literal) @fold

; Array literals
(array_literal) @fold

; Multi-line comments
(block_comment) @fold
